# K-LEO CHIP · 우주반도체 설계 시뮬레이션 프로젝트

## 목적과 현재 결과

저궤도 위성통신망에 적용할 메모리·보호회로의 오류 정정 및 복구 동작을
실험하고, 목표궤도 환경·부품 시험자료·검증계획·제작비와 연결한다.
V1.1은 기존 V1.0 메모리 적용성 도구에 비트 단위 동작 시뮬레이션을 추가했다.
반도체 소자나 트랜지스터의 물리 해석은 현재 구현 범위에 포함하지 않는다.

## 구현된 모듈

| 모듈 | 입력 | 결과 |
|---|---|---|
| 설계 실험실 | 64비트 데이터와 수동 오류 위치 | SECDED 신드롬·정정 위치·검출 신호, TMR 다수결·불일치, 데이터 손상 |
| 반복 오류 주입 | 1·2·3-bit 또는 상관 2-bit, 시행 수·시드 | 정상 데이터, 검출 후 중단 필요, 미검출 손상 횟수 |
| 환경 비교 | 500 km SSO / 888·1,280 km 42°, 수명·차폐 | 입력자료 기반 TID·원시 SEU·수치 여유 |
| 보호·복구 예비평가 | 오류율·워드 길이·점검주기·복구 가정 | 잔존 사건, 제한된 복구 중단시간 |
| 부품·시험자료 | 부품 사양·로트·환경 해석 출처 | 자료 공백과 검증계획 |
| 비용·양산 | 물량·단가·시험비·보호설계 | 반도체·장비 제작 및 검증비 |

## 5분 사용 순서

1. `dist/kleo-chip-standalone.html`을 브라우저에서 연다. 또는 `python start.py`를 실행한다.
2. 설계 실험실에서 `1-bit`를 누른다. SECDED는 정정, TMR은 정상 다수결을 확인한다.
3. `한 복제본 2-bit`를 누른다. SECDED는 정정 불가를 검출하며 TMR은 한 복제본의 두 오류를 가린다.
4. `두 복제본 동시`를 누르고 TMR을 선택한다. 다수결 데이터가 잘못되고 불일치 신호가 발생한다.
5. 이 상태에서 `다수결로 재동기화`를 누른다. 잘못된 다수결 값이 세 복제본에 전파되어
   불일치 신호가 사라지는 사례를 관찰한다. 임의로 정답을 복원하지 않는 모델이다.
6. `3-bit 취약 사례`에서 SECDED의 오정정과 TMR의 공통원인 취약성을 확인한다.
7. 반복 실험에서 시드·패턴을 지정하고 결과 CSV를 저장한다. 수동 주입 상태는 매 시행에 재사용하지 않는다.
8. 분석 대시보드에서 궤도·수명·차폐를 바꾸고 입력자료의 출처를 확인한다.
9. 시나리오 JSON을 저장하면 비트 상태·입력자료·실험 설정을 이어서 사용할 수 있다.

## 계산 범위와 분모

SECDED는 데이터 64 bit, 해밍 검사 7 bit, 전체 패리티 1 bit의 (72,64) 확장 해밍 부호다.
코드 위치는 1부터 시작하며 1·2·4·8·16·32·64가 해밍 검사 비트, 72가 전체 패리티다.
나머지 위치에 D0(최하위 비트)부터 순서대로 배치한다. 모든 1-bit 정정 및 모든 2-bit 검출을
검증했고, 3-bit 이상에서는 정정 보장이 없다. 오정정 후 `corrected` 신호가 발생하더라도
테스트벤치 원본과 다르면 미검출 데이터 손상으로 기록한다.

TMR은 3 × 64 bit를 비트별 다수결로 판독하며 별도의 불일치 검출 신호도 모델링한다.
투표기와 불일치 검출회로는 이상적이다. 두 복제본의 같은 비트가 바뀌면 데이터는 틀리지만
불일치를 감지할 수 있다. 세 복제본의 같은 비트가 바뀌면 불일치 신호도 없다.
이 결과는 개별 비트 다수결이므로 V1.0의 보수적인 워드 단위 TMR 확률모형과 동일하지 않다.

반복 실험의 분모는 구조별로 주입한 **오류 시행 횟수**다. 각 시행은 정상 메모리에서 시작한다.
독립 패턴은 구조의 전체 물리 저장 비트(64 / 72 / 192)에서 서로 다른 위치를 균등 추출한다.
상관 패턴은 TMR 두 복제본의 동일 비트이며, 보호 없음과 ECC에는 서로 다른 2비트를 주입한다.
같은 물리 비트 반전 개수로 비교하므로 저장 비트가 증가할 때 노출 사건 수도 증가하는 효과를
반영한 동일 플루언스 실험은 아니다. 시간 누적·입자 스펙트럼·물리 레이아웃을 모델링하지 않는다.

정상 데이터 비율은 임무 신뢰도, 궤도별 고장률 또는 위성망 가용도가 아니다.
이를 궤도별 원시 SEU율에 직접 곱하지 않는다. SECDED 저장 비트 오버헤드 12.5%, TMR 200%도
전체 칩 면적·전력 비율로 해석할 수 없다.

## 검증 결과

`node --test tests/*.test.mjs`로 핵심 계산과 자료처리를 검증한다.
현재 30개 테스트가 통과한다. SECDED의 72개 단일 오류 위치 전수 검사,
2,556개 이중 오류 위치 조합 전수 검사, TMR 192개 단일 오류 위치 전수 검사,
공통원인·오정정·오류 전파, 난수 재현성, 이전 JSON 호환성, 기존 선량·비용 모델을 포함한다.
브라우저 시각·상호작용 자동검사, 합성·타이밍·전력 분석, 방사선 조사시험은 수행하지 않았다.

## 다음 개발 단계

| 단계 | 구현 목표 | 필요한 입력·검증 |
|---|---|---|
| V1.2 | 시간 누적 오류·스크러빙 간격·인터리빙 실험 | 공간 상관 분포, SEU/MBU 정의, 동작 중 오류 추적 |
| V1.3 | 부품 시험 단면적과 외부 궤도 환경 연결 | 양성자 에너지·중이온 LET 반응, 스펙트럼·차폐·시기 |
| V1.4 | 합성 가능한 RTL과 FPGA 오류 주입 비교 | RTL 시뮬레이터, 타깃 FPGA, 자원·타이밍·전력 결과 |
| V2.0 | 통신 탑재체 기능·복구·군집 서비스 영향 | 탑재컴퓨터·모뎀·빔포밍 요구조건, 실제 상태·복구 로그 |

위 항목은 개발계획이며 현재 구현·검증 결과와 구분한다. 우주환경 수송이나 TCAD를 확장할 때는
SPENVIS·OMERE·Geant4 또는 검증된 반도체 해석 도구와 연결하고, PDK·부품 자료·시험조건을 고정한다.

## 방법론 참고자료

- NASA, *FPGA Mitigation Strategies for Critical Space Applications* (2018):
  https://ntrs.nasa.gov/citations/20180006778 — TMR과 복구 설계의 범위 검토용.
- ESA ESCIES, Radiation Tools:
  https://escies.org/webdocument/showArticle?groupid=6&id=743 — 외부 환경·방사선 해석 도구 안내.
- SPENVIS, models:
  https://www.spenvis.oma.be/models.php — 환경 및 방사선 영향 해석의 외부 연계 지점.

이 자료들은 방법론 참고이며 본 코드의 정확성 인증이나 초기 합성 수치의 출처가 아니다.
