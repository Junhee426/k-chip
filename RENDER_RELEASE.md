# K-LEO CHIP Render 패키지

- 애플리케이션 버전: V1.1.0
- 패키지 작성일: 2026-09-11
- 기반 소스 커밋: f12d9c641b034a8b95c2e6a1113cea20cb3e77ec
- 기반 소스 변경명: Add bit-level SECDED and TMR design experiments

## 변경 내용

- Render Static Site용 `render.yaml`, Node.js 버전 설정, 한국어 배포 안내 추가.
- 설치 의존성 없는 Node.js 빌드 추가. 배포할 정적 파일, 오프라인 실행본, 재빌드 가능한 소스 ZIP 생성.
- 소스 ZIP은 포함 파일 목록으로 구성하며 계정·인증정보·Git 이력·사이트 관리 메타데이터는 포함하지 않음.
- 기존 재패키징용 Python 명령은 공통 Node 빌드를 호출하도록 연결.
- 계산 모듈과 화면의 원본 JS·CSS·HTML은 유지.

## 배포 범위

이 산출물은 사용자가 자신의 GitHub/Render 계정에서 배포하는 코드 패키지다.
실제 Render 계정에 서비스 생성이나 배포를 실행한 결과는 포함하지 않는다.
앱의 계산·입력 범위와 과학적 한계는 README.md 및 PROJECT.md를 따른다.

## 이번 패키지 검증

- Node.js v24.19.0에서 기존 테스트 25개 통과(실제 비트 오류 전수검사·계산·입출력 포함).
- 새로 압축 해제한 소스만으로 재빌드 성공. 생성된 ZIP 바이트가 원본과 동일함을 확인.
- 7개 원본 앱 파일(JS·CSS·index.html)은 기반 커밋과 바이트 단위 동일함을 확인.
- 로컬 HTTP 서버에서 진입 페이지·모듈·CSS·오프라인 HTML·소스 ZIP 응답 200 확인.
- ZIP 무결성 및 소스 재생성 확인. 게시 폴더에 사이트 관리 메타데이터가 포함되지 않음을 확인.
- 브라우저 시각·상호작용 검사는 이번 변경에서 수행하지 않음. 실제 Render 배포 후 확인 절차는 배포 안내를 참조.
